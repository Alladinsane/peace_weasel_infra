name: Backup

on:
  schedule:
    - cron: "0 9 * * *" # daily, 09:00 UTC — prod
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to back up"
        required: true
        type: choice
        options: [prod]
        default: prod

concurrency:
  group: ops-${{ github.event.inputs.environment || 'prod' }}
  cancel-in-progress: false

jobs:
  backup:
    runs-on: [self-hosted, linux, ARM64, "env-${{ github.event.inputs.environment || 'prod' }}"]
    environment: ${{ github.event.inputs.environment || 'prod' }}
    steps:
      - name: Run backup
        shell: bash
        env:
          OCI_CLI_CONFIG: ${{ secrets.OCI_CLI_CONFIG }}
          OCI_CLI_KEY: ${{ secrets.OCI_CLI_KEY }}
        run: |
          set -euo pipefail
          cd /opt/wp-stack/docker

          mkdir -p ~/.oci
          printf '%s\n' "$OCI_CLI_CONFIG" > ~/.oci/config
          printf '%s\n' "$OCI_CLI_KEY" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem

          source <(grep -E '^(MYSQL_USER|MYSQL_PASSWORD|MYSQL_DATABASE)=' app.env)
          source <(grep OCI_BACKUP_BUCKET .env)

          docker compose build backup

          docker compose --profile tools run --rm \
            -v ~/.oci:/root/.oci:ro \
            -e MYSQL_USER="$MYSQL_USER" \
            -e MYSQL_PASSWORD="$MYSQL_PASSWORD" \
            -e MYSQL_DATABASE="$MYSQL_DATABASE" \
            -e MYSQL_HOST="db" \
            -e WP_CONTENT_PATH="/var/www/html/wp-content" \
            -e OCI_BACKUP_BUCKET="$OCI_BACKUP_BUCKET" \
            -e OCI_BACKUP_PREFIX="prod" \
            backup